def main() {
    print(fib(readint()));
}



def fib(n:int):int{
    if (n <= 1){
        if (n <= 0) {
            return 0;
        }
        return 1;
    } else {
        var x = fib(n-1): int;
        var y = fib(n-2): int;
        return x+y;
    }
}